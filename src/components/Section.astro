---
const { class: className, wide = false, id } = Astro.props;
---

<section id={id} class:list={["fade-in-section", className, { wide }]}>
	<slot />
</section>

<style>
	section {
		padding: 4rem 1.5rem;
		max-width: 800px;
		margin: 0 auto;
	}

    section.wide {
        max-width: 100%;
        padding: 4rem 2rem; /* slightly more padding on sides for wide */
    }
</style>

